# Build output
dist

# Dependencies
node_modules

# Astro generated types/cache
.astro

# Editor / OS
.DS_Store
Thumbs.db
*.swp

# Local secrets (contact form access key)
.env
.env.local

# Image-optimization cache (regenerated by `npm run optimize:images`)
scripts/.image-cache.json
